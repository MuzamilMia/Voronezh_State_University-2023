# # LAB 2 — FULL SOLUTION
# # Uses: pandas, sklearn, XGBoost
#
# import pandas as pd
# from sklearn.model_selection import train_test_split, cross_val_score
# from sklearn.compose import ColumnTransformer
# from sklearn.preprocessing import OneHotEncoder
# from sklearn.pipeline import Pipeline
# from sklearn.impute import SimpleImputer
# from sklearn.ensemble import RandomForestRegressor
# from sklearn.metrics import mean_absolute_error
# from xgboost import XGBRegressor
#
# # -----------------------------------------------------------
# # 1. Read dataset
# # -----------------------------------------------------------
# data = pd.read_csv("data.csv")
#
# print(data.columns.tolist())
# # -----------------------------------------------------------
# # 2. Select target column (y)
# # -----------------------------------------------------------
# y = data["House_Price"]
#
# # Remove target from X
# X = data.drop(["House_Price"], axis=1)
#
# # -----------------------------------------------------------
# # 3. Find categorical columns with cardinality ≤ 8
# # -----------------------------------------------------------
# low_cardinality_cols = [
#     col for col in X.columns
#     if X[col].dtype == "object" and X[col].nunique() <= 8
# ]
#
# # Also select numeric columns
# numeric_cols = [
#     col for col in X.columns
#     if X[col].dtype in ["int64", "float64"]
# ]
#
# # Final feature set
# X = X[low_cardinality_cols + numeric_cols]
#
# print("Categorical columns:", low_cardinality_cols)
# print("Numeric columns:", numeric_cols)
#
# # -----------------------------------------------------------
# # 5. Create SimpleImputer for numeric columns
# # -----------------------------------------------------------
# numeric_transformer = SimpleImputer(strategy="mean")
#
# # -----------------------------------------------------------
# # 6. Create Pipeline for categorical columns
# # -----------------------------------------------------------
# categorical_transformer = Pipeline(steps=[
#     ("imputer", SimpleImputer(strategy="most_frequent")),
#     ("onehot", OneHotEncoder(handle_unknown="ignore"))
# ])
#
# # -----------------------------------------------------------
# # 7. Create ColumnTransformer (preprocessor)
# # -----------------------------------------------------------
# preprocessor = ColumnTransformer(
#     transformers=[
#         ("num", numeric_transformer, numeric_cols),
#         ("cat", categorical_transformer, low_cardinality_cols)
#     ]
# )
#
# # -----------------------------------------------------------
# # 8. Final pipeline (preprocessing + RandomForestRegressor)
# # -----------------------------------------------------------
# model_rf = RandomForestRegressor(random_state=0)
#
# pipeline_rf = Pipeline(steps=[
#     ("preprocessor", preprocessor),
#     ("model", model_rf)
# ])
#
# # -----------------------------------------------------------
# # 9. Cross-validation with cross_val_score
# # -----------------------------------------------------------
# scores = -1 * cross_val_score(
#     pipeline_rf,
#     X, y,
#     cv=5,
#     scoring="neg_mean_absolute_error"
# )
#
# print("\nRandomForestRegressor Cross-Validation Results:")
# print("MAE scores:", scores)
# print("Mean MAE:", scores.mean())
#
# # -----------------------------------------------------------
# # 10. Split into train/test
# # -----------------------------------------------------------
# X_train, X_test, y_train, y_test = train_test_split(
#     X, y, test_size=0.2, random_state=0
# )
#
# # -----------------------------------------------------------
# # 11. Pipeline with XGBRegressor
# # -----------------------------------------------------------
# model_xgb = XGBRegressor(
#     n_estimators=500,
#     learning_rate=0.05,
#     random_state=0
# )
#
# pipeline_xgb = Pipeline(steps=[
#     ("preprocessor", preprocessor),
#     ("model", model_xgb)
# ])
#
# # -----------------------------------------------------------
# # 12. Fit, predict, compute MAE
# # -----------------------------------------------------------
# pipeline_xgb.fit(X_train, y_train)
# preds = pipeline_xgb.predict(X_test)
#
# xgb_mae = mean_absolute_error(y_test, preds)
# print("\nXGBRegressor Test MAE:", xgb_mae)
#
# # -----------------------------------------------------------
# # 13. Conclusion
# # -----------------------------------------------------------
# print("\n=== Conclusion ===")
# print(f"RandomForest MAE (CV mean): {scores.mean():.2f}")
# print(f"XGBoost MAE (test): {xgb_mae:.2f}")
#
# if xgb_mae < scores.mean():
#     print("XGBRegressor performed better because gradient boosting reduces bias.")
# else:
#     print("RandomForest performed better, likely due to lower variance and robustness.")